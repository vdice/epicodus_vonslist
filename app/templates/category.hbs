<h2>
  {{model.name}}
</h2>

<ul>
  {{#each model.listings as |listing|}}
    <li>
      <h4>
        {{#link-to 'listing' listing.id}}
          {{listing.title}}
        {{/link-to}}
      </h4>
    </li>
  {{/each}}
</ul>
