<h2>Categories:</h2>
<ul>
  {{#each model.categories as |category|}}
    <li>
      {{#link-to 'category' category.id}}
        {{category.name}}
      {{/link-to}}
    </li>
  {{/each}}
</ul>

{{search-bar listings=model.listings search="search"}}
